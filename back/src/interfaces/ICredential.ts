interface ICredential {
  id: number; // ID numérico que identifica al par de credenciales
  username: string; // Username del usuario que posee las credenciales
  password: string; // Password del usuario que posee las credenciales
}

export default ICredential;
